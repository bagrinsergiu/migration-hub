# ============================================================
# migration-hub — environment variables template
# Copy to .env and set your values: cp .env.example .env
# ============================================================

# --- Client DB (MCP dbhub-client-readonly) ---
# Used for reading data from the client database
MB_DB_HOST=your_client_db_host
MB_DB_USER=your_client_db_user
MB_DB_PASSWORD=your_client_db_password
MB_DB_NAME=your_client_db_name
MB_DB_PORT=5432
# MB_DB_TYPE=postgres  # or mysql — inferred from port if omitted

# --- Application database (MG_*) ---
MG_DB_HOST=localhost
MG_DB_NAME=migration_db
MG_DB_USER=root
MG_DB_PASS=your_password
MG_DB_PORT=3306

# --- Migration server ---
# Local: http://localhost:8080
# Shared network (migration_shared): http://mg_migration:80
MIGRATION_API_URL=http://localhost:8080

# --- Dashboard (URL for webhook and handshake) ---
# URL the migration server container uses to reach the dashboard.
# Shared network: DASHBOARD_BASE_URL=http://mb_dashboard:80
# DASHBOARD_WEBHOOK_BASE_URL=  # optional; defaults to DASHBOARD_BASE_URL
DASHBOARD_BASE_URL=http://localhost:8088

# --- Cache ---
CACHE_PATH=var/cache

# --- Google Sheets API (optional) ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:8088/api/google-sheets/oauth/callback
# GOOGLE_SYNC_INTERVAL=300
