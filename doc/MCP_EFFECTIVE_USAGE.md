# Руководство по эффективному использованию MCP серверов

## Что нужно сделать для эффективной работы

### 1. ✅ Проверка установленных зависимостей

Все необходимые инструменты уже установлены:
- ✅ **ripgrep** (rg) - установлен в `/home/sg/.cargo/bin/rg`
- ✅ **uvx** - установлен через snap
- ✅ **npx** - установлен через nvm

### 2. ✅ Исправление путей в конфигурации

**Уже выполнено:**
- ✅ Путь для Serena обновлен на текущий проект
- ✅ Создан скрипт `.cursor-mcp-serena.sh` для запуска Serena

**Примечание:** Скрипты для dbhub (`dbhub-migration`, `dbhub-client-readonly`) находятся в другом проекте (`/home/sg/projects/MB-migration`). Если нужно использовать их для текущего проекта, создай аналогичные скрипты или обнови пути.

### 3. ✅ Правила для агента созданы

Создан файл `.cursor/rules/mcp-usage.mdc` с правилами эффективного использования всех MCP серверов.

**Важно:** Убедись, что Cursor загружает правила из `.cursor/rules/`. Правила должны применяться автоматически.

### 4. 🔄 Перезапуск Cursor

После обновления конфигурации:
1. Полностью закрой Cursor (все окна)
2. Запусти Cursor заново
3. Проверь логи в `~/.config/Cursor/logs/*/exthost/anysphere.cursor-mcp/`

## Стратегия использования MCP серверов

### Приоритет 1: Serena (семантический анализ)
**Используй для:**
- Поиск классов, функций, методов
- Навигация по коду
- Понимание структуры проекта
- Точечное редактирование

**Пример:**
```
Вместо: grep "class MyClass"
Используй: find_symbol("MyClass")
```

### Приоритет 2: Ripgrep (быстрый поиск)
**Используй для:**
- Поиск точных строк
- Подсчет вхождений
- Поиск с фильтрами по типам файлов

**Пример:**
```
Вместо: grep -r "TODO"
Используй: mcp_ripgrep_search("TODO")
```

### Приоритет 3: Базы данных
**Используй для:**
- Работа с данными миграций (dbhub-migration)
- Чтение клиентских данных (dbhub-client-readonly)
- Поиск схемы БД

### Приоритет 4: Playwright
**Используй для:**
- Тестирование веб-интерфейса
- Автоматизация браузера
- Создание скриншотов

### Приоритет 5: GitMCP
**Используй для:**
- Получение документации
- Понимание архитектуры проекта

## Чек-лист для проверки

- [ ] Все зависимости установлены (ripgrep, uvx, npx)
- [ ] Пути в `~/.cursor/mcp.json` указывают на правильный проект
- [ ] Правила в `.cursor/rules/mcp-usage.mdc` созданы
- [ ] Cursor перезапущен после изменений
- [ ] MCP серверы подключены (проверь логи)

## Проверка работоспособности

После перезапуска Cursor проверь доступность инструментов:

1. **Serena:** Попробуй найти символ: `find_symbol("DatabaseService")`
2. **Ripgrep:** Выполни поиск: `mcp_ripgrep_search("TODO")`
3. **Базы данных:** Выполни простой запрос: `SELECT 1`
4. **Playwright:** Проверь доступность инструментов браузера
5. **GitMCP:** Получи документацию: `fetch_migration_hub_documentation()`

## Оптимизация производительности

1. **Используй обзоры перед детальным чтением:**
   - `get_symbols_overview()` → `read_file()` (если нужно)

2. **Используй подсчет перед полным поиском:**
   - `count-matches()` → `search()` (если нужно только количество)

3. **Фильтруй поиск:**
   - Используй `filePattern` в Ripgrep для ограничения области

4. **Комбинируй инструменты:**
   - Serena для навигации → Ripgrep для текстового поиска
   - GitMCP для контекста → Serena для кода

## Устранение проблем

### MCP сервер не подключается
1. Проверь логи: `~/.config/Cursor/logs/*/exthost/anysphere.cursor-mcp/`
2. Проверь, что команда в `mcp.json` работает в терминале
3. Проверь права доступа к скриптам

### Инструменты недоступны
1. Убедись, что Cursor полностью перезапущен
2. Проверь, что MCP сервер запущен (логи)
3. Проверь версии зависимостей

### Низкая производительность
1. Используй фильтры в Ripgrep
2. Используй `get_symbols_overview` вместо полного чтения файла
3. Кэшируй результаты через память Serena (`write_memory`)

## Дополнительные ресурсы

- Документация MCP: `doc/MCP_SETUP.md`
- Правила использования: `.cursor/rules/mcp-usage.mdc`
- Конфигурация: `~/.cursor/mcp.json`